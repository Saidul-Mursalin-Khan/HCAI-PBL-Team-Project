These result JSON files were generated using a 30,000-row random subset of the AG News training set (baseline classifier + all downstream tasks), for fast iteration. Task 1's own baseline_results.json (92.17% accuracy) was trained on the FULL 120,000-row training set separately. Re-run `python manage.py run_expert_sim`, `run_deferral`, and `run_active_learning` after re-running `train_baseline` on the full dataset to refresh all numbers consistently before final submission.